{$SITE_DOMAIN} {
	import /etc/caddy/domains/gate.conf
	encode zstd gzip
	request_body {
		max_size 22MiB
	}
	header {
		Strict-Transport-Security "max-age=31536000"
		-Server
	}
	# Caddy 设置可信转发头；web 不向宿主机公开 8000 端口。
	reverse_proxy web:8000
}

import /etc/caddy/domains/*.caddy
