# DESIGN.md — 电子书整理工作台（桌面端）

> 本文件定义桌面整理软件（PySide6）的视觉与交互语言。AI 修改 UI / 交互时以此为准。
> 设计来源：研究 `awesome-design-md` 仓库中 **Airtable**（友好、结构化数据、克制主色 CTA、状态语义色）与 **Notion**（圆角按钮 8px / 卡片 12px、胶囊状态徽章、柔和表面、语义成功/警告/错误色）后，结合「中文商用桌面工具」场景提炼。

## 1. 设计原则
- **商用而非开发**：所有面向用户的文案、状态、按钮一律中文；不向普通用户暴露 JSON / 报错堆栈 / 英文技术词。
- **看得见的状态**：每本书的「封面 / 网盘 / 网站」用绿色 ✓ / 灰色 — 即时可见；任务有进度条、已运行时间、中文状态。
- **一键优先，手动兜底**：主操作是「一键全自动」；旧的分步按钮保留但弱化为次级。
- **不阻塞**：资料不全的书进「待处理」队列，不影响其余书自动跑完。
- **克制的单一主色**：品牌绿用于主 CTA；状态用红/橙/绿语义色，不滥用彩色。

## 2. 颜色 Token
```
canvas        #f5f7f4   应用背景（柔和暖白）
surface       #ffffff   卡片 / 面板 / 输入框
surface_soft  #eef2ee   次级表面、标签页未选中、进度条底
surface_alt   #e7efe9   表格选中行
primary       #1f7d54   品牌绿（主按钮、选中标签）
primary_press #176343   主按钮按下
on_primary    #ffffff
ink           #1d2922   标题 / 强文本
body          #3c473e   正文
muted         #727d73   占位 / 次要 / 缺省状态(—)
hairline      #d8e0d8   细分隔线
border        #c2cdc4   输入框 / 次级按钮描边
success       #1a9e54   成功 / 已完成 / ✓
success_bg    #e4f6ec
warning       #d98324   警告 / 待处理
warning_bg    #fcf1de
error         #d64545   异常 / 失败
error_bg      #fbe7e7
info          #2563c9   链接 / 信息态（如已授权面板）
info_bg       #e7eefb
```
状态列配色：✓ → `success`；— → `muted`。

## 3. 字号与字体
- 字体：`Microsoft YaHei`；表格 / 正文 13px，标题 15px 600，按钮 13px 500。
- 行高宽松，留白充足；避免大段密集文字。

## 4. 圆角与间距
- 按钮 / 输入框：8px（md）
- 卡片 / 分组框 / 表格：12px（lg）
- 状态徽章 / 胶囊标签：9999px（full）
- 间距基准 4 的倍数；分组框内边距 12–16px。

## 5. 组件
### 按钮
- 主按钮：`primary` 实底白字、8px 圆角、padding 9×14。（如「一键上传选中(全自动)」「开始全自动流水线」）
- 次级按钮：`surface` 底 + `border` 描边、8px 圆角（如筛选/导入/导出/删除/分步上传）。
- 禁用态：`border` 底白字。

### 书库表格（核心界面）
- 列：书名 / 作者 / 主分类 / 子分类 / ISBN / 检测 / 分类 / 封面 / 网盘 / 网站 / 编号。
- 「封面 / 网盘 / 网站」三列用 ✓(绿) / —(灰)，让用户一眼看出每本书走到哪一步。
- 顶部筛选栏：搜索 + 封面(有/无) + ISBN(有/无) + 作者(有/无) + 主分类 + 状态(已传网盘/已同步网站/已排除/异常/待处理)。
- 批量栏：全选筛选结果 / 一键上传选中(全自动) / 一键上传全部已筛选(全自动) / 导入表格回填 / 导出已上传清单 / 从工作区移除。

### 任务与异常
- 顶部「任务进度」分组框：当前任务名 + 已运行秒数 + 进度条（进行中不确定态，结束置满）。
- 进度日志用中文；历史任务显示中文状态与摘要，不显示原始 JSON。

### 连接与分类设置
- 顶部「授权状态」面板：检查后显示「✅ 夸克已授权 账号：… 容量：… 环境通过」或「✅ 网站连接正常 编号：…」。
- 「选择夸克目录」按钮：弹窗列出顶层目录供选择；CLI 不支持列举时给出「去网页端复制文件夹 fid」的可操作指引。

## 6. 交互反馈（强制）
- 任何耗时操作前弹确认框说明范围与法律立场；点「是」后用进度条 + 状态栏实时反馈。
- 操作成功/失败用 `QMessageBox` 中文摘要，不抛技术异常原文给用户。
- 暂停/取消随时可用；已完成的书断点续传，不重做。

## 7. Do / Don't
- Do：状态用颜色+文字；主操作一键完成；资料不全的进待处理不阻塞；中文优先。
- Don't：不要向用户显示 JSON / 英文报错 / 技术栈细节；不要用多种高饱和色抢主色；不要把分步按钮当主路径。

## 8. 设计系统参考（来源）
- `awesome-design-md/design-md/airtable/DESIGN.md` — 友好数据美学、克制主色、状态语义。
- `awesome-design-md/design-md/notion/DESIGN.md` — 圆角几何、胶囊徽章、柔和表面、语义状态色。
- 仓库位置：`.study/awesome-design-md/`（仅学习参考，不打包进产物）。
